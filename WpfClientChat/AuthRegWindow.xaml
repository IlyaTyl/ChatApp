<Window x:Class="WpfClientChat.AuthRegWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClientChat"
        mc:Ignorable="d"
        Title="AuthRegWindow" Height="250" Width="300" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Логин:" Grid.Row="0" />
        <TextBox x:Name="usernameBox" Grid.Row="1" Margin="0,0,0,10"/>

        <Label Content="Пароль:" Grid.Row="2"/>
        <PasswordBox x:Name="passwordBox" Grid.Row="3" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Войти" Width="100" Margin="5" Click="Login_Click"/>
            <Button Content="Регистрация" Width="100" Margin="5" Click="Register_Click"/>
        </StackPanel>
    </Grid>
</Window>
